/**
 * Lightweight shape documentation for the JavaScript frontend.
 * These names mirror the service interfaces so the code can move to
 * TypeScript later without changing component boundaries.
 */
export const EvidenceStatuses = ["Verified", "Needs Review", "Unsupported"];
export const MessageSeverities = ["success", "info", "warning", "error"];
export const ProcessingStatuses = ["Pending", "Running", "Complete", "Warning", "Error"];
